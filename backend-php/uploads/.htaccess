# Patient uploads must NEVER be directly web-accessible.
# They are served only through the signed-URL endpoint /api/v1/files/{token}.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
